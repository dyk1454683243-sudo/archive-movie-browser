{
  "name": "archive-movie-mcp",
  "version": "0.1.0",
  "description": "MCP server for finding and watching public-domain films on the Internet Archive",
  "type": "module",
  "private": true,
  "bin": { "archive-movie-mcp": "server.mjs" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test"
  },
  "engines": { "node": ">=22" },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
